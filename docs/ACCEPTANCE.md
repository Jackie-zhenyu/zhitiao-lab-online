# 智调 Lab · 实际验收与交付

## 单次真实 AI 联调补充（2026-09-06）

在本机正式Streamlit页面、公开 PI A 闭环仿真数据上，用户明确授权问题“分析实验A”及0–21秒必要摘要后，完成北京 `qwen-plus` 的真实工具循环。使用原项目SDK和受限工具，无测试替身，不发送整份CSV。机器可读记录见 [live-ai-summary.json](acceptance/live-ai-summary.json)。

| 核验项 | 本轮实际结果 |
| --- | --- |
| 数据与确认 | 闭环仿真、非实测；1051行、0–21秒；`t0=1s,r0=0,r1=1,y0=0` |
| 模型请求 | 3次请求、3次响应；只对应一个用户问题 |
| 工具执行 | `profile_data`、`analyze_run`、`detect_events` 各1次成功；分析与事件工具显式请求0–21秒 |
| 证据校验 | 第二次模型响应未通过；有限纠正1次后第三次通过，最终状态success |
| 数据质量 / 事件 | 质量问题0条、规则事件0条；不能据此证明无故障 |
| 已有失败 | 更换完整本地密钥前曾有1次认证失败，无工具、无回答；独立保留，不计为成功 |
| 计数边界 | 两次操作共4次真实模型请求，均不计入701项离线测试；没有据此增加离线通过数量 |

结果可追溯ID：质量 `result-db5b2cd210f24780bc87ec6f0afb7c4f`，分析 `result-8293a637205e4589870e1b11df05b052`，事件 `result-f70e2623482b42bf934198496bdbc599`。源SHA-256为 `15bccb267450ad1e8deefa94cbb7909aa8430f1b0542eff1cc20906cfdf2d1e3`。实际分析指标为：

| 指标键 | 工具数值 | 单位与区间 |
| --- | ---: | --- |
| `rmse_t` | 0.22841650252730472 | 1；0–21秒 |
| `iae` | 2.495371903162585 | 1·s；0–21秒 |
| `tail_bias` | 0.0020176513823605205 | 1；0–21秒选段的配置末段 |
| `rise_time` | 6.10661623679659 | s；确认阶跃，阈值时间差 |
| `overshoot_pct` | 0 | %；确认阶跃观察段 |
| `settling_time` | 11.482420282338806 | s；相对于t0，在本次观测区间内满足 |

这些一般跟踪值覆盖0–21秒，不能替换A/B共同1–21秒指标。真实多轮“只看2～5秒”、AI发起的 `compare_runs`、其他模型/地域与开放式答案质量仍未验收；不将单个问题内部的3次模型往返说成用户多轮追问。原数值模块与解释边界保持不变。下方阶段记录保留其当时的无模型配置/0次真实API状态。

### 本轮交付离线回归与媒体

实际命令 `.venv\Scripts\python.exe -m pytest -q --junitxml=work/pytest-live-delivery.xml`，结果 **701 passed in 73.94s**，0失败/错误/跳过；日志 `work/pytest-live-delivery.log` 与对应XML保留。该次完整运行独立于初版的701项/212.50秒，不将二者相加，也不将4次真实模型请求列为离线测试通过项。

打包白名单更新后的独立回归实际 **22 passed in 7.83s**，`pip check` 无依赖冲突；独立22项不与本轮全量701项相加。

新版文件集中于 `outputs/submission-live/`，原 `outputs/submission/` 初版因PDF查看占用而完整保留。新版PDF为16页、765024字节，SHA-256为 `d7fde709ace81f481a023438fd6192d3dbd36d59c60035d3b9227a1d241dbe7c`；350个文本块边界检查通过，修改页1/9/10/14/16重新渲染并逐页查看，其余11页PNG像素与原已逐页验收版一致，第14页显示本轮701项/73.94秒。新版视频266.58秒、5321767字节，1280×720/24fps/H.264/AAC，由12张真实截图与25段本地中文旁白制作。视频全片音视频解码退出0，10章抽帧已查看，未人工试听。本轮记录见 [live-delivery-summary.json](acceptance/live-delivery-summary.json)，规格与提交材料映射见 [SUBMISSION](SUBMISSION.md)。

## 第八阶段增量验收（2026-09-06）

本地项目包、恢复核验、历史快照管理和会话清除已实现，格式与操作见[PROJECTS](PROJECTS.md)，完整阶段记录见[PROGRESS](PROGRESS.md)，机器可读证据见[stage8-summary.json](acceptance/stage8-summary.json)。以下第七阶段内容作为历史保留。

- 全量命令：`.venv\Scripts\python.exe -m pytest -q --junitxml=work/pytest-stage8-final.xml`。最终 **672 passed in 132.36s**，0失败/错误/跳过，退出0。新增107项，原565项保留。自动测试阻断真实网络，真实API调用0次。
- 覆盖项目往返、编码/毫秒/断点、同名变化、原始字节/字段顺序/基线/规则/A/B窗口、ZIP/JSON资源限制、结果篡改和越权字段、凭据拒绝、旧AI证据剥离、坏包保留旧会话、历史报告隔离、冻结提示、历史载入/删除和完整清空。
- 仅将旧测试的上传器数量更新为已实现的CSV和项目两个入口；数值断言未放宽。安全回归先复现A/B报告字段和ZIP目录分配问题，修复前记录保留。浏览器发现的入口及上传残留问题已修复并重新跑全量。
- `pip check`、`scripts/start.cmd --check-only`、compileall均成功；服务实际完整重启，健康端点200/ok，仅127.0.0.1:8501。依赖未变，本轮没有重新下载依赖；干净安装证据见第七阶段。
- CUA真实浏览器完成解析示例与阶跃、保存快照、中文及脚本字样备注、生成和下载事件；另将正式AppTest页面导出流程生成的公开PI A/B包通过真实文件选择上传、校验、恢复。恢复B1051行/21s、上升1.07547s、调节1.93851s、两份快照和共同20s窗口，明确闭环仿真来源。历史报告仅JSON归档，AI未配置并禁用；已查看桌面截图。
- 清空后入口、名称、备注与两个上传控件复位，实验/快照/曲线消失；重新上传仍可恢复。浏览器下载接口不提供文件路径，本轮没有验证下载目录中那一文件的字节回读；实际恢复使用独立生成的正式项目包。
- 核心13模块SHA-256不变，Git索引仍为空、无远程；环境、项目包和运行输出被忽略。未提交、推送或部署。

建议人工复验：分析自己的实验→生成并实际下载项目包→停止服务→重启→上传同一下载文件→校验恢复→核对摘要、单位、区间、初值和规则→改阈值核对旧响应/AI证据失效→重新生成包。删除历史快照后旧A/B结果应撤销，清空会话不影响下载文件。勿将真实密钥写入备注或CSV。

仍待单独验证：物理断网从磁盘重开HTML、真实API联调、Docker和Linux/macOS运行、多人负载。自动保存、数据库、活动聊天恢复及跨版本迁移未实现；项目包未加密，摘要不能鉴定设备条件或来源真实性。

## 第七阶段历史记录

验收日期：2026-09-06。目标是整理本地交付、修复失效/安全问题并回归，不增加计算功能。实际环境：Windows x64、Python 3.12.14、项目`.venv`。读取AGENTS、SPEC、PROGRESS、METRICS及全部既有测试后执行；已有524项测试全部保留。

## 已实现和本次修复

- 已实现：有界CSV导入、字段/单位确认、质量及连续选段、独立指标、现象证据、会话内A/B、受限AI工具与授权、冻结报告三格式。详细边界见README和对应定义文档。
- 字段、时间/物理/控制单位、映射或质量确认变化后，原来可能沿用异常规则的限幅/变化率勾选；现在重置规则与人工确认，切回旧单位也不复活。正常跨页仍保留同一上下文配置。新增五类回归先实际失败，再修复通过。
- AI对比引用原来把响应区间也标成第一个阶跃后原生点；现在响应从确认的t0标注，跟踪仍从实际原生样本开始。仅更正证据元数据，不改值或积分。
- 只有Key、但其余模型配置不完整时，原报告可能漏脱敏非典型格式Key；现在独立、有界读取当前项目配置用于脱敏，AI继续禁用。测试用明确假凭据验证HTML/JSON/CSV。
- Windows `.cmd` 曾打印错误却退出0；已修复显式传播子进程退出码，并通过真正CMD调用回归。项目虚拟环境为链接/目录联接到外部目录时拒绝安装，避免改动其他环境。
- `core/`的13份Python文件与阶段开始的SHA-256完全一致；旧测试文件未修改。METRICS仅改“报告尚未实现”的历史能力说明，公式、阈值和算法定义未改。

## 实际自动测试记录

| 命令或检查 | 实际结果 | 原始记录（本机忽略目录） |
| --- | --- | --- |
| `.venv\Scripts\python.exe -m pytest -q --junitxml=work/pytest-stage7-baseline.xml` | 基线524项，0失败/错误/跳过；JUnit累计28.279s | `work/pytest-stage7-baseline.xml` |
| `.venv\Scripts\python.exe -m pytest -q --junitxml=work/pytest-stage7-final.xml` | **565 passed in 117.75s**，退出0；0失败/错误/跳过 | `work/pytest-stage7-final.xml` |
| 新增最终流程测试 | 9项；原本地406项保留后共415 passed in 23.01s | `work/pytest-stage7-local-final.xml` |
| 新增证据/安全测试 | 15项；相关Agent/报告共133 passed in 20.94s | `work/pytest-stage7-evidence.xml` |
| 新增安装/启动脚本测试 | 16 passed in 1.15s，包含真实CMD失败退出码检查 | `work/pytest-stage7-delivery.xml` |
| `.venv\Scripts\python.exe -m pip check` | No broken requirements found，退出0 | 最终命令输出及汇总 |
| `.venv\Scripts\python.exe -m compileall -q app.py core ui agent reports examples tests scripts` | 退出0 | 最终命令输出及汇总 |

另新增一项20→10→20秒窗口、连续重跑与多次页面往返的回归，单项1 passed in 10.89s，记录`work/pytest-stage7-ab-navigation-probe.xml`；最终流程文件共10项。

上述相关测试集合有重叠，不能相加当成额外通过数量。最终总数为565。汇总、原记录SHA-256和源码核对见 [实际记录JSON](acceptance/stage7-summary.json)，日志/JUnit保留在本地`work/`，不将环境或运行日志加入版本控制。

覆盖包括独立 ln(9)、−ln(0.02) 和0%理论基准；RMSE/IAE手算；负向/非零初值/非等间隔/短记录/波动/未达/断点；编码、缺字段、非数值、无穷、读取上限；会话隔离；AI白名单、越权实验、伪造/旧证据、超时循环、格式错误、诱导文本与缺Key；报告值/配置一致、中文、缺指标、无AI、HTML和CSV注入及合法负数。

`tests/conftest.py`对每项测试清空LLM_API_KEY并禁止真实socket连接；模型只使用测试文件中的明确替身或MockTransport。AppTest验证正式页面和计算链路；其上传操作以文件流替换上传控件返回值，真实浏览器文件选择另验。**真实API调用次数为0，不计入离线通过数。**

## 安装、服务与代码保护

- `scripts/install.cmd`在已有项目环境退出0，核对完整57个精确版本；另在空的`work/stage7-install-clean/.venv`实际重新安装57包成功、pip check及导入成功。见`work/stage7-existing-install.log`、`work/stage7-clean-install.log/.json`。
- `scripts/start.cmd --port 8507`实际启动，根页与`/_stcore/health`均200、正文ok，只监听127.0.0.1。试验服务核对身份后停止；最终8501服务也已完整重启并核对。见`work/stage7-runtime-verification.json`和汇总。
- `scripts/start.cmd --check-only`退出0。最终启动实际执行项目Python的`-m streamlit run app.py`；没有请求模型或修改系统Python/ExecutionPolicy。
- 当前PowerShell5.1禁止`.ps1`，因此交付`.cmd`入口；不是通过修改系统策略解决。
- Linux CPython3.12 x86_64 wheel dry-run成功，57包可解析对应manylinux/通用wheel；此为Windows上的依赖可获取性检查，**Linux/macOS实机运行未验证**。
- Dockerfile和排除配置已生成、静态审查；**Docker CLI不可用，未执行构建或容器运行**。
- Git索引为空、无远程。候选文件检查未发现`.env`、venv、缓存、私人CSV；5份公开CSV均匹配清单。凭据模式仅命中两处明确测试字符串；这不等于能够识别任意未知秘密。未git add/commit/push，也未购买服务或部署公网。

## 浏览器验收与人工确认

本次使用实际可用的浏览器工具，操作本机Streamlit，不用AppTest冒充浏览器。逐项实际操作见汇总中的`browser`字段：

- 首次空会话、新CSV真实文件选择、UTF-8 BOM/分号识别、毫秒映射、121行/6s质量与阶跃、同名新内容撤销旧映射/曲线；实际新摘要与文件一致。
- 问题示例产生23条现象证据，点击缺失时间戳数据行34的证据可聚焦原始记录，界面说明不跨断点连接。
- 分别确认/保存PI A与B，计算20s共同窗口；改10s后旧结果撤销，再确认20s并计算。AI区无配置、聊天和发送禁用，无虚假回复。
- A/B报告成功生成；原0–21s跟踪与共同1–21s跟踪明确分区。页面和HTML原跟踪A RMSE为0.22841650252730472；共同窗口A/B RMSE为0.23298670253502995/0.11032715634575808。响应上升A/B为6.10661623679659/1.0754745393395544s，区间/结果ID有据可查。
- 原生浏览器可访问性树显示HTML内8组真实渲染的Plotly曲线及中文单位/阈值标记。恶意script结束标签备注作为文字显示，无弹窗；三个下载按钮均实际收到下载事件。已实际查看本轮桌面截图。
- 浏览器工具的DOM摘要曾漏掉可见A/B范围选项，截图和原生可访问性确认该项存在；没有为工具的遗漏修改应用。额外AppTest验证窗口恢复、连续重跑和导航保留。

第六阶段另有390×844截图、恶意节点检查和iframe内缩放/复位、自包含资源验证，历史记录见PROGRESS；这些不是物理断网后文件重开验证。本次没有重新声明完成窄屏或物理断网重开。

请在自己的电脑补充这条完整路径：

1. 按README安装启动。加载“一阶解析阶跃响应”，确认单位1和s、基线及单次阶跃。主区应有质量、四项跟踪及三项响应，响应约1.538s/0%/2.739s；这是tau=0.7s解析合成示例，和tau=1s理论测试区别明确。
2. 下载示例后重新上传；核对映射和单位。换同名不同内容文件、改s/ms或物理单位后，旧确认/图表撤销；改区间或响应阈值后需要重新确认阶跃。物理限幅/变化率确认不能沿用旧单位。
3. 加载问题示例并确认，查看阻断问题和原始行；点击证据聚焦，不能跨缺失区间连成连续分析。选择有效片段后再看局部指标。
4. 按DEMO保存PI A/B、确认共同20s并比较；改为10s或改负载后旧对比撤销。A对比自身，可用差值为0，零分母百分比为空；不宣布绝对优胜。
5. 无模型时AI区域应显示未配置且无假回复。若本地自行配置并授权，再做“分析A→只看2～5秒→与B比较”的**单独真实联调**；观察实际工具与新result_id。配置改变后旧聊天事实失效，不能进入新报告。不要把Key发到聊天中。
6. 报告页生成快照，核对数值、单位、行/时间区间、阈值、版本和ID。修改参数后保留旧快照并提示更新；重新生成才能下载新结果。没有AI结果显示AI未参与。
7. 下载HTML/JSON/CSV；用非秘密测试备注`</script><script>alert(1)</script>`核对只显示文字。关闭网络后从本机重新打开下载HTML，验证悬停、缩放、复位。**浏览器工具此前被安全策略拒绝file://，未绕过；此物理断网重开项目仍需人工确认。**
8. 窄屏核对侧栏展开/折叠、中文换行和报告表格滚动；多设备/不同浏览器实测属于人工补充范围。停止再启动服务应需重新加载，会话不是持久数据库。

## 未完成与已知限制

- 真实Qwen联调、账号权限、结构化答案服从率和问答质量未验证；没有真实配置/授权，未消耗真实API。
- Docker构建/运行、Linux/macOS实机、物理断网文件重开、多用户压力/长时间会话与部署访问控制未验证。默认本地版；公开演示要求见DEPLOYMENT。
- Word/PDF、复杂频域/多变量控制分析、长期持久化、设备连接/自动PID下发、身份登录不在已实现范围。
- 最大误差/超调只基于采样值；显示降采样可能漏尖峰；目标调节仅在当前观测内成立。没有异常不证明正常，条件未知A/B只能描述。JSON超过32MiB明确拒绝。
- Streamlit存在默认值与Session State同时设置的开发警告，当前功能回归通过；代码热重载可能留下旧Pydantic类，须完整重启并重新确认，不能靠取消校验解决。
- 固定版本不等于各平台已验收，也未提供wheel哈希/镜像digest的不可变供应链锁定；安装需包源或完整本地缓存。非英文平台控件仍可能含少量Streamlit英文文字。
